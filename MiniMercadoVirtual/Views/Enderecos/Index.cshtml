@model IEnumerable<MiniMercadoVirtual.Models.Cliente>

@{
    ViewData["Title"] = "Clientes";
}
<p>
    <h3>Endereços</h3>
</p>
<p>
    <a asp-action="Cadastrar" class="btn btn-primary">Novo</a>
</p>
<table class="table col-md-12">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                Endereços
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Cep</th>
                                <th>Logradouro</th>
                                <th>Número</th>
                                <th>Bairro</th>
                                <th>Cidade</th>
                                <th>Uf</th>
                                <th>Complemento</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var endereco in item.Endereco)
                            {
                                <tr>
                                    <td>@Html.DisplayFor(modelItem => endereco.Cep)</td>
                                    <td>@Html.DisplayFor(modelItem => endereco.Logradouro)</td>
                                    <td>@Html.DisplayFor(modelItem => endereco.Numero)</td>
                                    <td>@Html.DisplayFor(modelItem => endereco.Bairro)</td>
                                    <td>@Html.DisplayFor(modelItem => endereco.Cidade)</td>
                                    <td>@Html.DisplayFor(modelItem => endereco.Uf)</td>
                                    <td>@Html.DisplayFor(modelItem => endereco.Complemento)</td>
                                    <td><a class="btn btn-primary btn-sm" asp-action="Alterar" asp-route-id="@endereco.Id">Editar</a> | <a class="btn btn-danger btn-sm" asp-action="Excluir" asp-route-id="@endereco.Id">Excluir</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </td>
            </tr>
        }
    </tbody>
</table>
